"""Sub-agent Python modules (Part 2 / Block S).

Each deterministic-tier sub-agent is runnable as `python -m financex.subagents.<id>`.
The backend dispatcher spawns the module with argv[1] = JSON input; the module
prints a JSON output to stdout.
"""
